<div class="row">
        <a href="application/add">Add new user</a>

        <?php
        foreach($users as $user)
        {

            print '<div>'.$user->getId() . ' ' . $user->getFullName() . ' <a href="application/edit/' . $user->getId() . '">Edit</a> <a href="application/delete/' . $user->getId() . '">Delete</a> </div>';
        }
        ?>

</div>
